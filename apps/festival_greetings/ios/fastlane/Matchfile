git_url(ENV.fetch("MATCH_GIT_URL", "git@github.com:YOUR_ORG/certificates.git"))
storage_mode("git")
type("appstore")
app_identifier([ENV.fetch("APP_IDENTIFIER", "com.ruancanghui.FestivalGreetings")])
username(ENV.fetch("APPLE_ID", "developer@example.com"))
